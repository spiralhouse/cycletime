/**
 * Agent system - stub for proof of concept
 */

// Placeholder for agent system
export interface Agent {
  id: string;
  type: string;
  name: string;
  enabled: boolean;
}

export const agents = [];