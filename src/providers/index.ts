/**
 * Provider system - stub for proof of concept  
 */

// Placeholder for provider system
export interface Provider {
  id: string;
  type: string;
  name: string;
  enabled: boolean;
}

export const providers = [];