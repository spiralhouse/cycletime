ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        host = "0.0.0.0"
        host = ${?HOST}
        
        # Development mode configuration
        watch = []
        watch = ${?KTOR_WATCH_PATHS}
        
        # Auto-reload configuration for development
        autoreload = true
        autoreload = ${?KTOR_AUTORELOAD}
        
        # Development watch paths
        development = true
        development = ${?KTOR_DEVELOPMENT}
    }

    application {
        modules = [ io.spiralhouse.cycletime.ApplicationKt.module ]
    }
    
    # Development-specific configuration
    development {
        # Watch these paths for automatic reload
        watchPaths = [
            "build/classes/kotlin/main",
            "src/main/kotlin",
            "src/main/resources"
        ]
        watchPaths = ${?KTOR_DEV_WATCH_PATHS}
        
        # Automatic reload on source changes
        autoreload = true
    }
}

database {
    # Default to H2 with PostgreSQL compatibility mode for SPI-439 H2 integration
    url = "jdbc:h2:file:./cycletime-ce;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1"
    url = ${?DATABASE_URL}
    driver = "org.h2.Driver"
    driver = ${?DATABASE_DRIVER}
    maxPoolSize = 10
    logging = false
    logging = ${?DATABASE_LOGGING}
}

mcp {
    server {
        name = "cycletime"
        version = ${?CYCLETIME_VERSION}
        description = "CycleTime Project Orchestration MCP Server (Kotlin)"
    }
}
