package io.spiralhouse.cycletime.domain

/**
 * Domain-level constants used throughout the domain layer.
 */
object DomainConstants {
    /**
     * Standard UUID string length in canonical format (xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx).
     */
    const val UUID_STRING_LENGTH = 36
}