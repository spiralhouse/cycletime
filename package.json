{
  "name": "jcvd",
  "version": "0.1.0",
  "description": "Project orchestration framework that extends Claude Code",
  "private": true,
  "scripts": {
    "commitlint": "commitlint --edit",
    "commitlint-ci": "commitlint --from=HEAD~1 --to=HEAD --verbose",
    "commitlint-pr": "commitlint --from=$(git merge-base origin/main HEAD) --to=HEAD --verbose"
  },
  "devDependencies": {
    "@commitlint/cli": "^18.4.3",
    "@commitlint/config-conventional": "^18.4.3"
  },
  "keywords": [
    "claude-code",
    "project-orchestration",
    "developer-tools",
    "kotlin",
    "mcp"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/spiralhouse/jcvd.git"
  },
  "license": "MIT"
}